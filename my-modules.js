// js file that exports its code; node has buildin modules
// input a module to node COMMON JS => require();
                        // ES modules => import/export syntax 

// Assume this file services a module
// initially returns empty object so add code to be exported
module.exports = {
    Name : "Emmanuel"
}